#!/usr/bin/env sh
set -e

docker run \
    --rm \
    --network none \
    --mount type=bind,src="${PWD}",dst=/opt/app \
    --mount type=tmpfs,dst=/tmp \
    --workdir /opt/app \
    --entrypoint /opt/app/bin/test \
    sbtscala/scala-sbt:eclipse-temurin-jammy-22_36_1.10.1_3.4.2
